## What's New

### Local file logging

All CLI commands and workspace operations are now logged to `~/.grove/grove.log` for local debugging. Logs rotate automatically (1 MB, 3 backups). No data leaves the machine.

Use `--verbose` for DEBUG-level detail.
